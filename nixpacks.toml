# Nixpacks configuration for Toolix (Astro + Bun WASM App)
[providers]
providers = ["bun"]

[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun astro preview --host 0.0.0.0 --port ${PORT:-3000}"
